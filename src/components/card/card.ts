import { HTMLAttributes, ReactNode } from "react";
/**
 * Card props
 */
type CardProps = {
  /**
   * Children
   */
  children: ReactNode;
  /**
   *  title
   */
  title: string;
};

export { CardProps };
